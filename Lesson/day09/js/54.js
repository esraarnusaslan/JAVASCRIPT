const names = ['john', 'angela', 'albert', 'mark', 'melissa'];
const lastName = ['doe', 'al', 'messy', 'jackson', 'jack'];

//! FOR EACH
/* const cb = (animal) => {
    console.log(animal);
};
names.forEach(cb); */

//! FOR EACH SHORT WAY
const cb = (animal) => {
    console.log(animal);
};
names.forEach((names) => {
    console.log(names);
});
